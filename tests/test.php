<?php

use WecarSwoole\Util\File;

include './base.php';

// $f = fopen(File::join(STORAGE_ROOT, "big_file.csv"), 'rw');
// for ($i = 0; $i < 200000; $i++) {
//     fputcsv($f, ['张三', 12, '深圳', '中国', '篮球']);
// }
// fclose($f);